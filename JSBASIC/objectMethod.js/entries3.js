let product ={
    name:"IPhone 14 pro",
    company:"Apple",
    price:"12500",
    warranty:"I year",
    color:"red"
    
}
let entries =Object.entries(product);
console.log(entries);

// if instead of just fetching keys or values separately , we want to fetch 